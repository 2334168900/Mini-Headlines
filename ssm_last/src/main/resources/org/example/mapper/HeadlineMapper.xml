<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.HeadlineMapper">

    <resultMap id="BaseResultMap" type="org.example.stu.Headline">
            <id property="hid" column="hid" />
            <result property="title" column="title" />
            <result property="article" column="article" />
            <result property="type" column="type" />
            <result property="publisher" column="publisher" />
            <result property="pageViews" column="page_views" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="version" column="version" />
            <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        hid,title,article,type,publisher,page_views,
        create_time,update_time,version,is_deleted
    </sql>


    <select id="selectMyPage" resultType="java.util.Map">
        select hid,title,type, page_views as pageViews,TIMESTAMPDIFF(HOUR,create_time,NOW()) pastHours,publisher from news_headline where is_deleted=0
        <if test="portaivo.keyWords != null and portaivo.keywords.length()>0">
             and title like concat('%',#{portaivo.keyWords},'%')
        </if>
        <if test="portaivo.type !=0">
             and type = #{portaivo.type}
        </if>
    </select>



    <select id="selectAll" resultType="java.util.Map">
        select hid,title,article,type,tname as typeName,page_views as pageViews,TIMESTAMPDIFF(HOUR,create_time,NOW()) pastHours,publisher,nick_name as author FROM
            news_headline nh
                LEFT JOIN
            news_type nt ON nh.type = nt.tid
                LEFT JOIN
            news_user nu ON nh.publisher = nu.uid
            where hid=#{hid}
    </select>
</mapper>
